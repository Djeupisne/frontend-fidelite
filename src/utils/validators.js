import { z } from 'zod';
export const loginSchema = z.object({ email: z.string().email('Email invalide'), password: z.string().min(6, 'Minimum 6 caractères') });
const personSchema = { nom: z.string().min(1, 'Requis'), prenom: z.string().min(1, 'Requis'), age: z.coerce.number().min(1, 'Age invalide'), pays: z.string().min(1, 'Requis'), ville: z.string().min(1, 'Requis'), quartier: z.string().min(1, 'Requis'), religion: z.string().optional(), niveauEtude: z.string().optional(), profession: z.string().optional(), nomMere: z.string().min(1, 'Requis'), nomPere: z.string().min(1, 'Requis'), nomAine: z.string().min(1, 'Requis'), nomBenjamin: z.string().min(1, 'Requis') };
export const registerSchema = z.object({ email: z.string().email('Email invalide'), password: z.string().min(6, 'Minimum 6 caractères'), confirmPassword: z.string().min(6, 'Requis'), ...personSchema, partnerNom: z.string().min(1, 'Requis'), partnerPrenom: z.string().min(1, 'Requis'), partnerAge: z.coerce.number().min(1, 'Age invalide'), partnerPays: z.string().min(1, 'Requis'), partnerVille: z.string().min(1, 'Requis'), partnerQuartier: z.string().min(1, 'Requis'), partnerReligion: z.string().optional(), partnerNiveauEtude: z.string().optional(), partnerProfession: z.string().optional(), partnerNomMere: z.string().min(1, 'Requis'), partnerNomPere: z.string().min(1, 'Requis'), partnerNomAine: z.string().min(1, 'Requis'), partnerNomBenjamin: z.string().min(1, 'Requis'), dateRencontre: z.string().optional(), lieuRencontre: z.string().optional() }).refine(d => d.password === d.confirmPassword, { message: 'Les mots de passe ne correspondent pas', path: ['confirmPassword'] });
export const profileSchema = z.object({ ...personSchema });
export const passwordSchema = z.object({ currentPassword: z.string().min(1, 'Requis'), newPassword: z.string().min(6, 'Minimum 6 caractères'), confirmNewPassword: z.string().min(1, 'Requis') }).refine(d => d.newPassword === d.confirmNewPassword, { message: 'Les mots de passe ne correspondent pas', path: ['confirmNewPassword'] });
